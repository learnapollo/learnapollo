machine:
  node:
    version: 6

dependencies:
  override:
    - npm install

test:
  override:
    - echo 'No tests yet'

deployment:
  surge:
    branch: [master, dev]
    commands:
      - npm run deploy

